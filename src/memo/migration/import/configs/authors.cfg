[transmogrifier]
include = memo_migration_base
pipeline -=
  constructor
  schemaupdater
pipeline +=
  constructor
  schemaupdater

[jsonsource]
tables = ['${definitions:table_prefix}author']

[portal_type]
value = string:Author

[foldering]
value = string:${definitions:path_prefix}/authors

[settitle]
blueprint = collective.transmogrifier.sections.manipulator
keys = name
destination = string:title
condition = python:'title' not in item